import { TypeCharactCategory } from './type-charact-category';

describe('TypeCharactCategory', () => {
  it('should create an instance', () => {
    expect(new TypeCharactCategory()).toBeTruthy();
  });
});
